#TODO Create a Prometheus configuration file that scrapes metrics 
#from the FastAPI app's /metrics endpoint every 15 seconds

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: beans-api
    static_configs:
      - targets: ["host.docker.internal:8000"]
